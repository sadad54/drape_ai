// Outfit suggestions screen
import 'package:flutter/material.dart';

class OutfitSuggestionsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Outfit Suggestions Screen')),
    );
  }
}
