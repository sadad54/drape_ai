// Virtual try-on screen
import 'package:flutter/material.dart';

class VirtualTryonScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Virtual Try-On Screen')),
    );
  }
}
