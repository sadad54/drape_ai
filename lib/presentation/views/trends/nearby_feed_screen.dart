// Nearby feed screen
import 'package:flutter/material.dart';

class NearbyFeedScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Nearby Feed Screen')),
    );
  }
}
