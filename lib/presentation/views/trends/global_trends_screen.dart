// Global trends screen
import 'package:flutter/material.dart';

class GlobalTrendsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Global Trends Screen')),
    );
  }
}
