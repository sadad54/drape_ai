// Personality quiz screen
import 'package:flutter/material.dart';

class PersonalityQuizScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Personality Quiz Screen')),
    );
  }
}
