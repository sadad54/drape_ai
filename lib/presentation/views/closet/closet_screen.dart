// Closet screen
import 'package:flutter/material.dart';

class ClosetScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Closet Screen')),
    );
  }
}
